import React from "react";

const Login = () => (
  <main>
    <p>Log in to use</p>
    <p>
      <a href="/api/auth/github">Sign in with github</a>
      <a href="/api/auth/arena">Sign in with are.na</a>
    </p>
  </main>
);

export default Login;
